services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=gymsync_db'
      - 'POSTGRES_USER=postgres'
      - 'POSTGRES_PASSWORD=postgres'
    ports:
      - '5433:5432'
